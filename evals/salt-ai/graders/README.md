# Deterministic graders

Case files declare stable grader/check IDs. Executable compile, type,
interaction, retrieval, and scan graders are added with the corresponding
delivery surface; blind human adjudication remains separate.
