import budgets from "./catalogBudgets.json";

// Token-owned artifacts remain below the remediation baseline while allowing
// ordinary catalog evolution without freezing the current generated byte size.
export const TOKEN_OWNED_ARTIFACT_BYTE_BUDGET = budgets.tokenOwnedArtifactBytes;
