# Superseded source-intersection decisions

The archived Call outline and solid decisions in [the accompanying JSON](intersection-decisions-before-contour-update.json) describe an earlier drawing. The September 2026 contour update changed their SVGs, recipe hashes, generated joins and discovered candidates. The current audit confirmed that these are real artwork changes, not line-ending-only differences.

The two old decisions have been removed from the active `junction-reviews.json` map and retained here as history. Both current icons remain pending in the catalogue migration; no new approval has been granted. The final-paint corner review remains the current completeness check.
