// These legacy Solid exports have no enclosed object surface to fill. Keep
// their established open arrows instead of inventing a heavier line or head.
// New identical variants still require an explicit catalogue decision.
export const lineOnlySolidVariants = new Set(["import", "export"]);
