/**
 * Phase 0 skeleton for Salt cell-state classes (proposal §4.4).
 *
 * In 2.x these are app-level helpers — `.error-cell`, `.warning-cell`,
 * `.success-cell`, `.error-row`, `.warning-row`, `.success-row` etc. — that
 * live in `ag-cell-states.css` and `ag-row.css`. They will move into this
 * part's CSS in Phase 1+. Currently a no-feature stub.
 */
import { createPart } from "ag-grid-community";

export const saltCellStates = createPart({});

