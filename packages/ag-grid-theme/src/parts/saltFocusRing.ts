/**
 * Phase 0 skeleton for the unified Salt focus ring part (proposal §4.4).
 *
 * Salt's focus-ring CSS currently lives across several files in
 * `packages/ag-grid-theme/css/parts/`. Phase 1+ consolidates it here so that
 * focus rings ship only when the part is registered (no scoping hacks needed).
 */
import { createPart } from "ag-grid-community";

export const saltFocusRing = createPart({});

