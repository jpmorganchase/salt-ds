import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { Card, InteractableCard } from "@salt-ds/core";
import { withFlexGap } from "docs/decorators/withFlexGap";
import {
  CharacteristicUsage,
  CSSClassTable,
} from "css-variable-docgen-components";

<Meta
  title="Documentation/Core/Card"
  component={Card}
  parameters={{
    viewMode: "docs",
  }}
/>

# Card

Cards contain content and actions about a single subject.

## Card Component

This default example is intended to show the way the Card component’s content can be implemented.

<Canvas>
  <Story id="core-card--default" />
</Canvas>

## Interactable Card

Cards can have actions by making use of `InteractableCard`. We discourage the use of any focusable elements within interactable Cards.

<Canvas>
  <Story id="core-card--interactable" />
</Canvas>

## Disabled Card

Interactable cards can have disabled styling applied by setting `disabled={true}`.

Make sure to as well apply `disabled={true}` to any nested components, such as Text, to ensure disabled styling is also applied to the inner content.

<Canvas>
  <Story id="core-card--interactable-disabled" />
</Canvas>

## Multiple Cards

Layout components can be used for grouping Card components together.

<Canvas>
  <Story id="core-card--cards-in-grid-layout" />
</Canvas>

# API

```
import { Card, InteractableCard } from "@salt-ds/core";
```

## Interactable Card Props

<ArgsTable of={InteractableCard} />

## CSS Class

<CSSClassTable of={Card} />

## Characteristics Used

<CharacteristicUsage of={Card} />
