import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { Card, InteractableCard } from "@salt-ds/core";
import { withFlexGap } from "docs/decorators/withFlexGap";
import {
  CharacteristicUsage,
  CSSClassTable,
} from "css-variable-docgen-components";

<Meta
  title="Documentation/Core/Card"
  component={Card}
  parameters={{
    viewMode: "docs",
  }}
/>

# Card

A Card is a visually distinct container that displays a snapshot of information about a single subject, and acts as an entry point to more detailed information.

## Card Component

This default example is intended to show the way the Card component’s content can be implemented.

<Canvas>
  <Story id="core-card--default" />
</Canvas>

## Interactable Card

**Deprecated:** In previous versions, Cards could have interactable styling by setting `interactable={true}`. These cards could be wrapped in Link, for example, for interactivity. We discourage the use of any focusable elements within interactable Cards.

**Updated:** Cards can have actions by making use of the `InteractableCard` component. We discourage the use of any focusable elements within interactable Cards.

<Canvas>
  <Story id="core-card--interactable-as-link" />
</Canvas>

## Disabled Card

**Deprecated:** In previous versions, Cards could have disabled styling applied by setting `disabled={true}`.

**Updated:** Interactable cards can currently have disabled styling applied by setting `disabled={true}`.

Make sure to also apply `disabled={true}` to any nested components, such as Text, to ensure disabled styling is also applied to the inner content.

<Canvas>
  <Story id="core-card--interactable-disabled" />
</Canvas>

## Multiple Cards

Layout components can be used for grouping Card components together.

<Canvas>
  <Story id="core-card--cards-in-grid-layout" />
</Canvas>

# API

```
import { Card, InteractableCard } from "@salt-ds/core";
```

## Card Props

<ArgsTable of={Card} />

## Interactable Card Props

<ArgsTable of={InteractableCard} />

## Card CSS Classes

<CSSClassTable of={Card} />

## Interactable Card CSS Classes

<CSSClassTable of={InteractableCard} />

## Characteristics Used in Card

<CharacteristicUsage of={Card} />

## Characteristics Used in Interactable Card

<CharacteristicUsage of={InteractableCard} />
