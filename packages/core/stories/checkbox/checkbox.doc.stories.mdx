import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { Checkbox, CheckboxGroup, CheckboxIcon } from "@salt-ds/core";
import {
  CharacteristicUsage,
  CSSClassTable,
} from "css-variable-docgen-components";
import HelpAndSupport from "docs/blocks/help-and-support.doc.mdx";

<Meta
  title="Documentation/Core/Checkbox"
  component={Checkbox}
  parameters={{
    viewMode: "docs",
  }}
/>

# Checkbox

A Checkbox allows the user to turn a specific value on or off. The value can be an independent value or a selection of one or more values from a given set of choices (structured as a Checkbox Group).

##### Use when

- You want to present an independent choice that the user can toggle on or off.
- You want to present a list of independent options where the user can select any number of choices.

##### Don't use when

- The checkbox displays a mutually exclusive choice between two or more values, in that case a `Radio Button` component is more appropriate.

## Features

### Variations

A Checkbox can be in one of two states, `unchecked` or `checked`

<Canvas>
  <Story id="core-checkbox--default" />
</Canvas>

### Indeterminate

A Checkbox with the prop `indeterminate="true"` will display the checkbox with an indeterminate icon, this is a third state separate from the `unchecked` and `checked` states.
The most common use case is a nested checkbox group which shows a hierarchy of selections. If a group contains a mix of selected and unselected
children, the parent will show an indeterminate state.

<Canvas>
  <Story id="core-checkbox--indeterminate" />
</Canvas>

### Validation state

A Checkbox with the prop `validationStatus="error"` will cause the checkbox to be shown with the error status colors.
Use this state when you want to alert the user of a critical issue that is related to the checkbox.

<Canvas>
  <Story id="core-checkbox--error" />
</Canvas>

A Checkbox with the prop `validationStatus="warning"` will cause the checkbox to be shown with the warning status colors.

<Canvas>
  <Story id="core-checkbox--warning" />
</Canvas>

- When used in a group, use `validationStatus` on the group rather than the individual controls. Any status provided to the group will take precedence over the statuses applied directly to the nested controls.
- Avoid using `validationStatus` when a Checkbox is `disabled`. If provided, `disabled` functionality and styling will take precedence over any validation status styling.

### Disabled

A Checkbox with the prop `disabled="true"` will suppress all functionality along with disabling the Checkbox.

<Canvas>
  <Story id="core-checkbox--disabled" />
</Canvas>

### Readonly

A Checkbox with the prop `readOnly="true"` will suppress all functionality along with displaying readonly styling.
**Note:** Read-only fields are not focusable since they are treated as a static representation rather than a form control.

<Canvas>
  <Story id="core-checkbox--readonly" />
</Canvas>

## Checkbox Group

A checkbox group allow selection of one or more values from a given set of choices. They can be aligned horizontally or vertically.

### Vertical group

<Canvas>
  <Story id="core-checkbox--uncontrolled-group" />
</Canvas>

### Horizontal group

The checkbox group layout can be set to horizontal by setting the `direction` prop to "horizontal"

<Canvas>
  <Story id="core-checkbox--horizontal-group" />
</Canvas>

### Wrapping

By default, the horizontal checkboxes will wrap onto the next line if the group doesn't fit within the container. There is the option to force the checkboxes to remain inline and wrap the label text instead by setting `wrap` prop to `false`.

`wrap=true`

<Canvas>
  <Story id="core-checkbox--wrap-group" />
</Canvas>

`wrap=false`

<Canvas>
  <Story id="core-checkbox--no-wrap-group" />
</Canvas>

### Controlled

A controlled checkbox group requires events to trigger the state change. The application must track state and update the props manually. This is useful for when logic or validation needs to be applied or a request should happen when updating a form.

<Canvas>
  <Story id="core-checkbox--controlled-group" />
</Canvas>

### Long Text Group

<Canvas>
  <Story id="core-checkbox--long-text-group" />
</Canvas>

## Configuring Checkbox

### API

```
import { Checkbox, CheckboxGroup } from "@salt-ds/core";
```

## Props

### Checkbox

<ArgsTable of={Checkbox} />

### Checkbox Group

<ArgsTable of={CheckboxGroup} />

## Related components

The Checkbox component is similar to the following components:

- [Radio Button](?path=/docs/documentation-core-radio-button--docs)

## Classes

### Checkbox

<CSSClassTable of={Checkbox} />

### Checkbox Group

<CSSClassTable of={CheckboxGroup} />

### Checkbox Icon

<CSSClassTable of={CheckboxIcon} />

## Characteristics used

### Checkbox

<CharacteristicUsage of={Checkbox} />

### Checkbox Group

<CharacteristicUsage of={CheckboxGroup} />

### Checkbox Icon

<CharacteristicUsage of={CheckboxIcon} />

## Accessibility

If you are using a Checkbox without a label:

- You should pass an `aria-label` to `Checkbox` that describes its purpose.

<HelpAndSupport />
