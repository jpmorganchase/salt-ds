import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { ComponentAnatomy } from "docs/components/ComponentAnatomy";
import { FlexLayout, FlexItem } from "@jpmorganchase/uitk-core";
import {
  CSSClassTable,
  CSSVariableTable,
} from "css-variable-docgen-components";
import { DefaultFlexLayout } from "./flex-layout.stories";

<Meta title="Layout/Docs/FlexLayout" component={FlexLayout} />

# Flex Layout

Dictates a horizontal or vertical order and direction for UI elements.
it can be used for cases in which the direction needs complex customization that can't be acchieved by stack or flow layouts.

Height is driven by child items,
Visual seperators can be turned on/off (default) between items

<Canvas>
  <Story id="layout-docs-flexlayout--default-flex-layout" />
</Canvas>

## API

```
import { FlexLayout, FlexItem } from "@jpmorganchase/uitk-lab";
```

## Props

**ResponsiveProp** refers to a prop than take either multiple values (one per breakpoint) or a single value that would take effect across all different screen sizes

```
direction={{ lg: "row", md: "column", sm: "column", xl: "row", xs: "row" }}
```

### Flex Layout Props

<ArgsTable of={FlexLayout} />

### Flex Item Props

<ArgsTable of={FlexItem} />

## Usage Examples

### Flex Item inside default flex layout

A flex item allows more complex controls to the default behaviour of a flex child, like defining the priority with which an element grows/shrink and it's alignment

<Canvas>
  <Story id="layout-flexlayout-flexitem--flex-item-wrapper" />
</Canvas>

### Responsive

The flex layout supports the use of responsive props, so you can defined different behaviours based on viewport size.
You can use the default breakpoints or pass in you own.

<Canvas>
  <Story id="layout-flexlayout--toolkit-flex-layout-responsive" />
</Canvas>

### Simple Usage

the simple usage examples shows a group of elements inside a flex container with `wrap`

<Canvas>
  <Story id="layout-flexlayout--flex-layout-simple-usage" />
</Canvas>

## Used in

The flex layout is used in the following components

- [Split layout](?path=/docs/layout-docs-splitlayout--default-split-layout)
- [Stack layout](?path=/docs/layout-docs-stacklayout--default-stack-layout)
- [Flow layout](?path=/docs/layout-flowlayout--default-flow-layout)
- [Parent Child layout](?path=/docs/layout-docs-parentchildlayout--default-parent-child-layout)

## Anatomy

<Canvas>
  <ComponentAnatomy showLegend>
    <FlexLayout>
      <FlexItem></FlexItem>
    </FlexLayout>
  </ComponentAnatomy>
</Canvas>

## classes

### Layout

<CSSClassTable of={FlexLayout} />

### Item

<CSSClassTable of={FlexItem} />
