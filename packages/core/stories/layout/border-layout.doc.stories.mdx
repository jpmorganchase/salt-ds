import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { ComponentAnatomy } from "docs/components/ComponentAnatomy";
import { BorderLayout, BorderItem } from "@jpmorganchase/uitk-core";
import {
  CSSClassTable,
  CSSVariableTable,
} from "css-variable-docgen-components";
import HelpAndSupport from "../help-and-support.doc.mdx";

<Meta
  title="Documentation/Core/Layout/BorderLayout"
  component={BorderLayout}
  subcomponents={{ BorderItem }}
  parameters={{
    viewMode: "docs",
  }}
/>

# Border layout

Defines the main content regions of an application, region or widget, such as a footer, header or side navigation.
The border layout is built using [Grid layout](?path=/docs/documentation-core-layout-gridlayout--page) and it is intended for laying out large sections of content, such as pages, where there is at least a main area with a header and footer.

<Canvas>
  <Story id="core-layout-borderlayout--border-layout-all-panels" />
</Canvas>

## Use Border layout when...

You want a fixed layout with a flexible main region.

### Don't use Border layout when...

You want a customised layout that you can control in both row and column dimensions. In this case we recommend you having a look at [Grid layout](?path=/docs/documentation-core-layout-gridlayout--page).

### Examples

#### Border Item inside default border layout

The border item wrapper allows you to select the right position for your content to be displayed at - `header`, `left`, `main`, `right` or `bottom`.  
It also provides access to other properties such as alignment and sticky behaviour.

<Canvas>
  <Story id="core-layout-borderlayout-borderitem--border-item-wrapper" />
</Canvas>

#### Fixed panels

You can have a responsive `main` region while the `left` and `right` panels remain with a fixed width as the content changes.

<Canvas>
  <Story id="core-layout-borderlayout--border-layout-fixed-panels" />
</Canvas>

#### Composite usage

The composite usage combines examples from other layout components nested to give a better example of a complex implementation of the border component.

<Canvas>
  <Story id="core-layout-borderlayout--border-layout-form-composite" />
</Canvas>

## Configuring Border layout

### API

```
import { BorderLayout, BorderItem } from "@jpmorganchase/uitk-core";
```

### Props

<ArgsTable of={BorderLayout} />

## Anatomy

<Canvas>
  <ComponentAnatomy>
    <BorderLayout style={{ border: "dashed 1px black", padding: "1rem" }}>
      <BorderItem style={{ border: "dashed 1px black", padding: "1rem" }} />
    </BorderLayout>
  </ComponentAnatomy>
</Canvas>

### Classes

#### Item

<CSSClassTable of={BorderItem} />

<HelpAndSupport />
