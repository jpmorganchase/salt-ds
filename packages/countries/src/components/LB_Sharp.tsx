// WARNING: This file was generated by a script. Do not modify it manually
import { forwardRef } from "react";
import { useId } from "@salt-ds/core";
import { clsx } from "clsx";

import { CountrySymbol, CountrySymbolProps } from "../country-symbol";

export type LB_SharpProps = CountrySymbolProps;

const LB_Sharp = forwardRef<SVGSVGElement, LB_SharpProps>(function LB_Sharp(
  props: LB_SharpProps,
  ref
) {
  const uid = useId(props.id);

  const { className, ...rest } = props;

  return (
    <CountrySymbol
      data-testid="LB_Sharp"
      aria-label="Lebanon"
      viewBox="0 0 72 50"
      ref={ref}
      className={clsx(className, { saltSharpCountrySymbol: true })}
      {...rest}
    >
      <mask
        id={`${uid}-LB-a`}
        x="0"
        y="0"
        maskUnits="userSpaceOnUse"
        style={{ maskType: "alpha" }}
      >
        <path fill="#D9D9D9" d="M0 0h72v50H0z" />
      </mask>
      <g mask={`url(#${uid}-LB-a)`}>
        <path fill="#DD2033" d="M0-11h72v72H0z" />
        <path fill="#F5F7F8" d="M0 41V9h72v32z" />
        <path fill="#008259" d="m44 32-8-19-8 19h6v5h4v-5h6Z" />
      </g>
    </CountrySymbol>
  );
});

export default LB_Sharp;
