import { forwardRef } from "react";

import { CountrySymbol, CountrySymbolProps } from "../country-symbol";

export type {{componentName}}Props = CountrySymbolProps;

export const {{componentName}} = forwardRef<SVGSVGElement, {{componentName}}Props>(
  function {{componentName}}(props: {{componentName}}Props, ref) {
    return (
      <CountrySymbol data-testid="{{componentName}}" aria-label="{{ariaLabel}}" viewBox="{{viewBox}}" ref={ref} {...props}>
        {{{svgElements}}}
      </CountrySymbol>
    );
  }
);
