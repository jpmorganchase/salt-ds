import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { Button } from "@salt-ds/core";
import {
  ExportIcon,
  NotificationIcon,
  ShareIcon,
  TearOutIcon,
} from "@salt-ds/icons";
import { Toolbar } from "@salt-ds/lab";
import { CollapsibleToolbarItem } from "./components";

<Meta title="Documentation/Lab/Toolbar" />

# Toolbar

Toolbar is a flexible container for controls.

<br />

Simple `Toolbar` with 10 `Buttons`. Note that Button text is not displayed if Button
contains an Icon.

<Canvas>
  <Story id="lab-toolbar--simple" />
</Canvas>

<br />

##### Instant Collapse

Example 1, use CSS alone to style compact state

<Canvas>
  <Story id="lab-toolbar--simple-with-collapsible-items" />
</Canvas>
