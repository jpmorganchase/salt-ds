import { Meta, Canvas, ArgsTable } from "@storybook/addon-docs";
import { ComponentAnatomy } from "docs/components/ComponentAnatomy";
import {
  Banner
} from "@jpmorganchase/uitk-lab";
import {
  CSSClassTable,
  CSSVariableTable,
  CharacteristicUsage,
} from "css-variable-docgen-components";

<Meta
  title="Documentation/Lab/Banner"
  component={Banner}
  parameters={{
    viewMode: "docs",
  }}
/>

# Banner

The Banner component lets your users know about an err, warning, a successfully completed or an information update. The banner should relate to the current workflow
and span the page or container horizontally to give quick and non-disruptive feedback.

# API

```
import { Banner } from "@jpmorganchase/uitk-lab";
```

## Props

<ArgsTable of={Banner} />

## HTML Anatomy of Badge component

<Canvas>
  <ComponentAnatomy>
    <Banner
      //eslint-disable-next-line no-script-url
      LinkProps={{ href: "javascript:void(0)" }}
    >
      Banners appear inline on the page
    </Banner>
  </ComponentAnatomy>
</Canvas>

## CSS Class

<CSSClassTable of={Banner} />

## Characteristics Used

<CharacteristicUsage of={Banner} />

## --uitkBanner CSS Custom Property API

<CSSVariableTable of={Banner} />
