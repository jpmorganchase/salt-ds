import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import {
  BorderLayout,
  BorderItem,
  GridLayout,
  GridItem,
  FlexLayout,
  FlexItem,
  FlowLayout,
  SplitLayout,
  StackLayout,
} from "@jpmorganchase/uitk-lab";

<Meta title="Layout/Doc" component={GridLayout} />

# Layout Components

Layout componentry was created to help you build consistent and responsive layouts while using Toolkit components.

## Border Layout

Defines the main content regions of an application, region or widget, such as a footer, header or side navigation.

The region is defined by the `position` prop.

<Canvas>
  <Story id="layout-borderlayout--toolkit-border-layout" />
</Canvas>

## API

```
import {BorderLayout, BorderItem} from "@jpmuitk/toolkit-odyssey";
```

## Props

<br />

### Border Layout

<ArgsTable story="Border Layout" />

### Border Item

<ArgsTable of={BorderItem} />

## Grid Layout

Defines an equally distributed layout system using columns and rows.

It defaults to a twelve column grid but you can customise it using the `columns` and `rows` props.

<Canvas>
  <Story id="layout-gridlayout--toolkit-grid-layout" />
</Canvas>

### API

```
import {GridLayout, GridItem} from "@jpmorganchase/uitk-lab";
```

### Props

<br />

### Grid Layout

<ArgsTable story="Grid Layout" />

### Grid Item

<ArgsTable of={GridItem} />

## Parent Child Layout

Displays a hierarchical structure comprising of a main content area and an accompanying parent region, used to drive the content that is displayed.

<Canvas>
  <Story id="layout-parentchildlayout--toolkit-parent-child-layout" />
</Canvas>

## API

```
import {ParentChildLayout} from "@jpmorganchase/uitk-lab";
```

## Props

<ArgsTable story="Parent Child Layout" />

## Flex layout

Flex layout is the internal base for all layout components that build from a container using Flexbox.

<Canvas>
  <Story id="layout-flexlayout--toolkit-flex-layout" />
</Canvas>

### API

```
import {FlexLayout, FlexItem} from "@jpmorganchase/uitk-lab";
```

### Props

#### Flex layout

<ArgsTable of={FlexLayout} />
#### Flex item
<ArgsTable of={FlexItem} />

## Flow layout

Dictates a horizontal order and direction for UI elements.

<Canvas>
  <Story id="layout-flowlayout--toolkit-flow-layout" />
</Canvas>

### API

```
import {FlowLayout} from "@jpmorganchase/uitk-lab";
```

### Props

<ArgsTable of={FlowLayout} />

## Stack layout

Defines left and right regions for UI elements within a span, such as a button bar.

<Canvas>
  <Story id="layout-stacklayout--toolkit-stack-layout" />
</Canvas>

### API

```
import {StackLayout} from "@jpmorganchase/uitk-lab";
```

### Props

<ArgsTable of={StackLayout} />

## Split layout

Defines left and right regions for UI elements within a span, such as a button bar

<Canvas>
  <Story id="layout-splitlayout--button-bar-in-split-layout" />
</Canvas>

### API

```
import {SplitLayout} from "@jpmorganchase/uitk-lab";
```

### Props

<ArgsTable of={SplitLayout} />

## Styling layout components

You can override a default style assignment in layout components, however, in some cases the styles are dynamically defined by JS values and overriding the CSS variable will not work.
