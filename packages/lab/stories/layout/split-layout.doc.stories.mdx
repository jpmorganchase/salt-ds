import { Meta, Story, Canvas, ArgsTable } from "@storybook/addon-docs";
import { ComponentAnatomy } from "docs/components/ComponentAnatomy";
import { SplitLayout } from "@jpmorganchase/uitk-lab";
import { FlexItem } from "@jpmorganchase/uitk-core";
import {
  CSSClassTable,
  CSSVariableTable,
} from "css-variable-docgen-components";
import { DefaultSplitLayout } from "./split-layout.stories";

<Meta
  title="Documentation/Lab/Layout/SplitLayout"
  component={SplitLayout}
  parameters={{
    viewMode: "docs",
  }}
/>

# Split layout

Defines left and right regions for UI elements within a span, such as a button bar.
By default the Split layout will break into a second row with the elements on the right hand side as soon as it finds limited space.
For layouts that require more than 2 regions please see [Flow layout](?path=/docs/layout-docs-lab-flowlayout--page).

<Canvas>
  <Story id="lab-layout-splitlayout--default-split-layout" />
</Canvas>

## How you can use Split layout

Use Split layout when there are 2 defined areas that require to be in the opposite horizontal ends of a container,
for example if 2 `ButtonBar` components need to be in a header.

## API

```
import { SplitLayout } from "@jpmorganchase/uitk-lab";
```

## Props

<ArgsTable of={SplitLayout} />

## Examples

### Simple Usage

The simple usage examples shows 2 groups of buttons inside a Split layout.

<Canvas>
  <Story id="lab-layout-splitlayout--split-layout-simple-usage" />
</Canvas>

## Anatomy

<Canvas>
  <ComponentAnatomy showLegend>
    <SplitLayout
      style={{ border: "dashed 1px black", padding: "1rem" }}
      leftSplitItem={
        <FlexItem style={{ border: "dashed 1px black", padding: "1rem" }} />
      }
      rightSplitItem={
        <FlexItem style={{ border: "dashed 1px black", padding: "1rem" }} />
      }
    />
  </ComponentAnatomy>
</Canvas>

### Classes

<CSSClassTable of={SplitLayout} />
