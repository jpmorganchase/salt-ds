import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { LinearProgress } from "@salt-ds/lab";
import {
  CharacteristicUsage,
  CSSClassTable,
} from "css-variable-docgen-components";

<Meta
  title="Documentation/Lab/Progress/Linear Progress"
  component={LinearProgress}
  parameters={{
    viewMode: "docs",
  }}
/>

# Linear Progress

A linear progress indicator gives the user an understanding of how long a system operation will take, indicated by a horizontal bar. It should be used when the operation will take more than a second to complete.

## Examples

### Default Linear Progress

<Canvas>
  <Story id="lab-progress-linear--default" />
</Canvas>

### Linear Progress with maximum value

You can specify the maximum value of the progress indicator. The percentage progress will then be calculated using this value. The default value is 100.

<Canvas>
  <Story id="lab-progress-linear--max-value" />
</Canvas>

### Linear Progress with progressing value

This is an example of a linear progress indicator that displays a progressing value.

<Canvas>
  <Story id="lab-progress-linear--progressing-value" />
</Canvas>

## How to use

### API

```
import { LinearProgress } from "@salt-ds/lab";
```

#### Props

<ArgsTable of={LinearProgress} />

#### Characteristics Used

<CharacteristicUsage of={LinearProgress} />

#### CSS Class

<CSSClassTable of={LinearProgress} />

## Accessibility

Improve accessibility by customizing the ariaLabel prop to provide additional context about what is loading. For example:

```
ariaLabel = “loading settings panel 0%”
```