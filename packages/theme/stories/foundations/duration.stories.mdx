import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Banner } from "@salt-ds/lab";
import { DocGrid } from "docs/components/DocGrid";
import { NumberBlock } from "docs/components/NumberBlock";

<Meta title="Theme/Foundations/Duration" />

# Duration

<Banner status="warning">Deprecated: Use Duration foundation instead.</Banner>

Components which may have a standard time duration between the moment a user performs an action
and the moment feedback is given by the system.

The duration in each case is measured in milliseconds.

## Tokens

<DocGrid>
  <NumberBlock number="100" cssVariable="--salt-duration-instant" />
  <NumberBlock number="300" cssVariable="--salt-duration-perceptible" />
  <NumberBlock number="1000" cssVariable="--salt-duration-notable" />
  <NumberBlock number="10000" cssVariable="--salt-duration-cutoff" />
</DocGrid>
