import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { Banner, BannerContent } from "@salt-ds/core";
import { DocGrid } from "docs/components/DocGrid";
import { NumberBlock } from "docs/components/NumberBlock";

<Meta title="Theme/Foundations/Delay" />

# Delay

<Banner status="warning">
  <BannerContent>Deprecated: Use Duration foundation instead.</BannerContent>
</Banner>

Components which may have a standard time duration between the moment a user performs an action
and the moment feedback is given by the system.

The delay duration in each case is measured in milliseconds.

## Tokens

<DocGrid>
  <NumberBlock number="100" cssVariable="--salt-delay-instant" />
  <NumberBlock number="300" cssVariable="--salt-delay-perceptible" />
  <NumberBlock number="1000" cssVariable="--salt-delay-notable" />
  <NumberBlock number="10000" cssVariable="--salt-delay-cutoff" />
</DocGrid>
