import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { NumberBlock } from "docs/components/NumberBlock";
import { DocGrid } from "docs/components/DocGrid";

<Meta title="Documentation/Styles and Theming/Foundations/Z-Index" />

# Z-Index

The z-index property specifies the stack order of a component. A component with a greater stack order is always in front of an element with a lower stack order.

Often used with Shadow to create visual priority, so users know where to direct their focus. The greater the distance, measured along the z-axis, the larger the shadow.

## Z-Index

<Canvas>
  <Story name="Z-Index">
    <DocGrid>
      <NumberBlock number="1" cssVariable="--salt-zIndex-default" />
      <NumberBlock number="1000" cssVariable="--salt-zIndex-popout" />
      <NumberBlock number="1050" cssVariable="--salt-zIndex-docked" />
      <NumberBlock number="1100" cssVariable="--salt-zIndex-appHeader" />
      <NumberBlock number="1200" cssVariable="--salt-zIndex-drawer" />
      <NumberBlock number="1300" cssVariable="--salt-zIndex-modal" />
      <NumberBlock number="1400" cssVariable="--salt-zIndex-notification" />
      <NumberBlock number="1420" cssVariable="--salt-zIndex-dragObject" />
      <NumberBlock number="1450" cssVariable="--salt-zIndex-contextMenu" />
      <NumberBlock number="1500" cssVariable="--salt-zIndex-flyover" />
    </DocGrid>
  </Story>
</Canvas>
