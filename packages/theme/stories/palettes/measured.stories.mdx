import {
  Meta,
  Story,
  Canvas,
  ColorPalette,
  ColorItem,
} from "@storybook/addon-docs";
import { Banner, BannerContent } from "@salt-ds/core";
import { ColorContainer } from "docs/components/ColorContainer";
import { DocGrid } from "docs/components/DocGrid";

<Meta title="Theme/Palettes/Measured" />

# Measured

<Banner status="warning">
  <BannerContent>
    Deprecated: Track characteristic should be used instead.
  </BannerContent>
</Banner>

Colors used to represent a measurement, value or progression.

## Base

<ColorContainer className="paletteContainer">
  <ColorPalette className="docs">
    <ColorItem
      title="Background"
      subtitle="**Deprecated:** --salt-palette-measured-background"
      colors={{
        "--salt-palette-measured-background":
          "var(--salt-palette-measured-background)",
        "--salt-palette-measured-border-background":
          "var(--salt-palette-measured-border-background)",
      }}
    />
    <ColorItem
      title="Border"
      subtitle="**Deprecated:** --salt-palette-measured-border"
      colors={{
        "--salt-palette-measured-border": "var(--salt-palette-measured-border)",
        "--salt-palette-measured-border-disabled":
          "var(--salt-palette-measured-border-disabled)",
      }}
    />
    <ColorItem
      title="Fill"
      subtitle="**Deprecated:** --salt-palette-measured-fill"
      colors={{
        "--salt-palette-measured-fill": "var(--salt-palette-measured-fill)",
        "--salt-palette-measured-fill-disabled":
          "var(--salt-palette-measured-fill-disabled)",
      }}
    />
    <ColorItem
      title="Foreground"
      subtitle="**Deprecated:** --salt-palette-measured-foreground"
      colors={{
        "--salt-palette-measured-foreground":
          "var(--salt-palette-measured-foreground)",
        "--salt-palette-measured-foreground-disabled":
          "var(--salt-palette-measured-foreground-disabled)",
        "--salt-palette-measured-foreground-active":
          "var(--salt-palette-measured-foreground-active)",
        "--salt-palette-measured-foreground-activeDisabled":
          "var(--salt-palette-measured-foreground-activeDisabled)",
      }}
    />
  </ColorPalette>
</ColorContainer>
