import { Meta, Canvas, Story } from "@storybook/addon-docs";
import { DocGrid } from "docs/components/DocGrid";
import { CursorBlock } from "docs/components/CursorBlock";

<Meta title="Theme/Characteristics/Draggable" />

# Draggable

Components that can be grabbed and moved to a target area (related to Target).

<Canvas>
  <Story name="Base">
    <DocGrid>
      <CursorBlock cursor="--salt-draggable-vertical-cursor-hover" />
      <CursorBlock cursor="--salt-draggable-vertical-cursor-active" />
      <CursorBlock cursor="--salt-draggable-horizontal-cursor-hover" />
      <CursorBlock cursor="--salt-draggable-horizontal-cursor-active" />
      <CursorBlock cursor="--salt-draggable-grab-cursor-hover" />
      <CursorBlock cursor="--salt-draggable-grab-cursor-active" />
    </DocGrid>
  </Story>
</Canvas>
