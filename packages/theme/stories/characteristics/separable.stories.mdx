import { Meta, Canvas, Story } from "@storybook/addon-docs";
import { DocGrid } from "docs/components/DocGrid";
import { ColorBlock } from "docs/components/ColorBlock";
import { NumberBlock } from "docs/components/NumberBlock";
import {
  LineBlock,
  LineBlockCell,
  LineBlockCode,
  LineBlockDotted,
} from "docs/components/LineBlock";

<Meta title="Theme/Characteristics/Separable" />

# Separable

Group of attributes to separate information/elements.
Connected to Container

- Level 1 - used when subtle division is needed/to divide similar items e.g. options within a list, individual comments
- Level 2 - used to divide related areas e.g. heading and body text
- Level 3 - used to separate areas of content that are not closely related e.g. app header from main content

### Base

<Canvas>
  <Story name="Base">
    <DocGrid>
      <LineBlockCell />
      <LineBlockCode>--salt-separable-borderStyle</LineBlockCode>
    </DocGrid>
  </Story>
</Canvas>

### Primary

<Canvas>
  <Story name="Primary">
    <DocGrid>
      <NumberBlock
        number="0.35"
        cssVariable="--salt-separable-primary-borderOpacity"
      />
      <ColorBlock colorVar="--salt-separable-primary-borderColor" />
    </DocGrid>
  </Story>
</Canvas>

### Secondary

<Canvas>
  <Story name="Secondary">
    <DocGrid>
      <NumberBlock
        number="0.25"
        cssVariable="--salt-separable-secondary-borderOpacity"
      />
      <ColorBlock colorVar="--salt-separable-secondary-borderColor" />
    </DocGrid>
  </Story>
</Canvas>

### Tertiary

<Canvas>
  <Story name="Tertiary">
    <DocGrid>
      <NumberBlock
        number="0.15"
        cssVariable="--salt-separable-tertiary-borderOpacity"
      />
      <ColorBlock colorVar="--salt-separable-tertiary-borderColor" />
    </DocGrid>
  </Story>
</Canvas>
