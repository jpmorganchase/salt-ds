---
"@salt-ds/core": minor
---

Added a `shape` prop to `Avatar` to switch the silhouette between `"round"` (default) and `"square"`. `shape="square"` can be useful when the Avatar represents an entity such as an organization, product or team, though round remains a valid choice in those cases too.
