---
"@salt-ds/lab": minor
---

Added `SidePanelCloseButton` component that handles close logic, icon rendering, and accessible labelling (`aria-label` + `aria-labelledby` linked to the panel title) automatically. Removes the need for consumers to manually wire up IDs, icons, and click handlers.
