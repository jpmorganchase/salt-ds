---
"@salt-ds/lab": minor
---

Removed `BreadcrumbsNext`, `BreadcrumbNext`, `BreadcrumbNextTrigger`, and `BreadcrumbNextLabel` from lab and promoted to core. As part of the promotion the components were renamed e.g., `BreadcrumbsNext` is now `Breadcrumbs`.

Removed the legacy `Breadcrumbs` and `Breadcrumb` exports from lab.
