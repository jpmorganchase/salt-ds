---
"@salt-ds/theme": minor
---

Added an `alpha-light-*` palette ramp, an always-white counterpart to the always-black `alpha-dark-*` ramp, and `--salt-focused-onSolid-outlineColor` for the focus outline of components placed on solid backgrounds.

Backfilled the `alpha-dark-*` ramp into the legacy theme.
