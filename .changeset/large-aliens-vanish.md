---
"@salt-ds/lab": minor
---

Added `BreadcrumbsNext`, `BreadcrumbNext`, `BreadcrumbNextTrigger`, and `BreadcrumbNextLabel` for accessible breadcrumb navigation. Supports wrapping, disclosure and inline expansion collapse modes, and routing integration via `render` props.
