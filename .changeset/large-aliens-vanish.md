---
"@salt-ds/lab": minor
---

Added `BreadcrumbsNext`, `BreadcrumbNext`, `BreadcrumbNextTrigger`, and `BreadcrumbNextLabel` for accessible breadcrumb navigation. Supports wrapping, disclosure collapse, and routing integration via `render` props. Provide `href` for the current item so it renders as a focusable link with `aria-current="page"`.
