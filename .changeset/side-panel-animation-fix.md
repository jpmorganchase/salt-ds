---
"@salt-ds/lab": patch
---

Fixed `SidePanel` animating on first render when using `defaultOpen`. The panel now appears immediately without animation when initially open, and only animates on subsequent open/close interactions.
