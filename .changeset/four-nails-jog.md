---
"@salt-ds/lab": minor
---

Removed `ToolbarNext`, `ToolbarContentNext`, and `TooltrayNext` from lab and promoted to core. As part of the promotion the components were renamed e.g., `ToolbarNext` is now `Toolbar`.

The legacy `Toolbar` and `Tooltray` exports in lab are unchanged.
