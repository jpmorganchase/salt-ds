---
"@salt-ds/theme": patch
---

Increase the low-density icon stroke to approximately 8/7 units so the primary stroke renders at 1 CSS pixel at the default 14px icon size. This aligns its apparent weight with default high, medium and touch icons while preserving secondary stroke proportions and local overrides.
