---
"@salt-ds/core": minor
---

Add `CardContent`, a layout primitive that owns the inner padding of a card. Use it when you want children of `Card`, `LinkCard` or `InteractableCard` (for example, a full-width image or divider) to extend to the card's edges. When a `CardContent` child is present, the wrapping card's default padding is automatically removed.
