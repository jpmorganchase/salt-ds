---
"@salt-ds/icons": minor
---

Added:

- `RemoveBookmarkIcon` and `RemoveBookmarkSolidIcon` for removing a saved bookmark.

Preserved the bookmark family's ribbon boundary and aligned the removal mark between outline and solid variants.
