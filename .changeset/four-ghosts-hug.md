---
"@salt-ds/core": patch
"@salt-ds/icons": patch
"@salt-ds/lab": patch
"@salt-ds/theme": patch
---

Updated `--salt-size-icon` in HD to be `12px` directly rather than using `max(...,12px)` when using it.
