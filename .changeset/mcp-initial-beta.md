---
"@salt-ds/mcp": minor
---

Publish the initial beta release of the public Salt MCP package.

- `@salt-ds/mcp` packages the canonical read-only Salt Design System MCP server with five public tools for repo context, exact reference lookup, create guidance, review, and migration guidance.

Treat the package as beta while the workflow contract, generated registry artifacts, and consumer setup continue to settle.
