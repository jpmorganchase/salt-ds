---
"@salt-ds/lab": minor
---

Updated `getTriggerProps` in `useSidePanel` to include a built-in `onClick` handler that toggles the panel open state, removing the need for consumers to manage toggle logic themselves, unless they want to.
