---
"@salt-ds/core": minor
---

Added `appearance` support to `Card`, `InteractableCard`, `LinkCard`, and `Panel`. Added `borderColor` support to `Card`, `InteractableCard`, and `LinkCard` default states.
