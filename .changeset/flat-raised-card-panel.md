---
"@salt-ds/core": minor
---

Added `elevation` support to `Card`, `InteractableCard`, `LinkCard`, and `Panel`. Added `borderColor` support to `Card`, `InteractableCard`, and `LinkCard` default states.
