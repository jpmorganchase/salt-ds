---
"@salt-ds/core": minor
---

Add a `render` prop to `CollapsiblePanel` for customizing its root element, with `data-open` and `data-closed` attributes for styling its state.

`CollapsiblePanel` animations now respect reduced motion preferences.
