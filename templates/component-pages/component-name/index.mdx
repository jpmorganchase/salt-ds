---
title: Component Name
data:
  description: "A concise one-to-three sentence summary of the component, the problem it solves, and why a team would use it."
  summary: "A concise, standalone summary suitable for search and generated Markdown."
  applicability: "Describe the supported packages, environments, and important constraints."
  stability: "stable"
  providers: ["SaltProviderNext"]
  deprecations: []
  relatedRecords: ["component:RelatedComponent"]
  provenance: "authored_normative_guidance"

  # To omit optional items, comment them out with #
  sourceCodeUrl: "https://github.com/..."
  package:
    name: "@salt-ds/core"
  alsoKnownAs: ["Other Name", "Another Name"]
  relatedComponents: [
      # Add a { name: "...", relationship: "..." } block for each
      # related component here (separated by commas).
      # Permitted values for relationship are: "similarTo" or
      # "contains".
      { name: "Similar Component", relationship: "similarTo" },
      { name: "Contained Component", relationship: "contains" },
    ]
  # stickerSheet: "https://figma.com/..."
  relatedPatterns: ["Pattern Name", "Another Pattern Name"]
# Leave this as is
keywords:
  $ref: "#/data/alsoKnownAs"
layout: DetailComponent
---

{/* This area stays blank */}
