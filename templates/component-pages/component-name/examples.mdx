---
title:
  $ref: ./#/title
layout: DetailComponent
sidebar:
  exclude: true
data:
  $ref: ./#/data
---

## Component example title

Brief description of this example.

If you omit it, the `exampleName` will be displayed in the list for this example
(with spaces inserted, so "ComponentExample" would display as
"Component Example").

### Best practices

See the readme for more details about this section. Delete if not applicable.

<LivePreview componentName="component-name" exampleName="ComponentExample" />

{/_ Declare this example in its authored manifest. The manifest must name the
complete local file closure, Salt packages, external dependencies, public
visibility, stability, and authored-example provenance. _/}

{/* Add more ...<LivePreview /> blocks here as needed */}
