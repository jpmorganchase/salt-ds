# Token aliases

Use `--consumer-border-default` instead of consuming the canonical container
border role directly. The alias remains backed by
`--salt-container-primary-borderColor` in `consumer-brand.css`.
