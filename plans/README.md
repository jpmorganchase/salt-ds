# Implementation Plans

Generated by the improve skill on 2026-08-20. Execute in the order below unless
dependencies say otherwise. Each executor should read the plan fully before
starting, honor its STOP conditions, and update the row when done.

## Execution order & status

| Plan | Title                                                      | Priority | Effort | Depends on | Status                                |
| ---- | ---------------------------------------------------------- | -------- | ------ | ---------- | ------------------------------------- |
| 001  | Add a composable Core List for passive and actionable rows | P1       | L      | —          | BLOCKED (Safari/VoiceOver QA pending) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale — finding fixed independently or approach
abandoned)

## Dependency notes

- Plan 001 is additive and has no prerequisite plan.

## Findings considered and rejected

- Create a separate `ActionList` instead of one `List` family: rejected for this
  requirement. Explicit `ListItemContent`, `ListItemAction`, and
  `ListItemActions` composition keeps the semantic variants visible without
  multiplying top-level list families.
- Repurpose, rename, or migrate a Lab component: rejected because this work is
  explicitly Core-only and must not depend on Lab.
- Use `ListBox`, Menu, or Toolbar semantics: rejected because the required rows
  are independent passive content, buttons, and links—not a selection or
  composite-navigation widget.
