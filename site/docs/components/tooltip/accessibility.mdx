---
# Leave the frontmatter as is
title:
  $ref: ./#/title
layout: DetailComponent
sidebar:
  exclude: true
data:
  $ref: ./#/data
---

The Tooltip does not receive focus, so we recommend that you do not include interactive elements as they will not be accessible.

If you need to display focusable content such as buttons or links, consider using the [Overlay](../overlay) component instead.
