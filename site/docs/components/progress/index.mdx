---
title: Progress
data:
  description: "`Progress` indicator gives the user a precise view of how long a system operation is taking. Use it when the action will last more than a second. Two variants are available to accommodate different layouts: `CircularProgress` and `LinearProgress`."
  sourceCodeUrl: "https://github.com/jpmorganchase/salt-ds/tree/main/packages/core/src/progress"
  package:
    name: "@salt-ds/core"
    initialVersion: "1.17.0"
  alsoKnownAs: [Progress bar, Progress indicator]
  relatedComponents: [{ name: "Spinner", relationship: "similarTo" }]
keywords:
  $ref: "#/data/alsoKnownAs"
layout: DetailComponent
---

{/* This area stays blank */}
