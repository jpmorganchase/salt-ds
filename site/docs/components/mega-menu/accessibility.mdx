---
title:
  $ref: ./#/title
layout: DetailComponent
sidebar:
  exclude: true
data:
  $ref: ./#/data
---

## Best practices

### Triggering mega menu

- Any button that triggers a Mega menu container.
- It should be wrapped in a `<nav>` element to establish a navigation landmark.
- The group of buttons should be structured as a `<ul>` or `<ol>`, with each button placed inside an `<li>`.
- `MegaMenuTrigger` automatically applies **aria-expanded** to reflect the expanded or collapsed state, and **aria-controls** to reference the associated mega menu panel.

### Mega menu panel

- `MegaMenuPanel` renders a `<div>` with `role="region"`. Always provide an `aria-label` on `MegaMenuPanel` that describes the menu content, for example `aria-label="Solutions menu"`. This ensures assistive technology users can distinguish the panel from other landmarks on the page.

### Custom region

- A custom region can include a locally customized component; however, it must comply with ADA standards. For example, opening another modal through an action in a custom region may violate ADA compliance and should be avoided.

### Accessibility semantics

- The Mega menu container should use **role="region"**.
- Mega menu groups should use **Link lists**, structured as `<ul>` elements containing `<li>` items.
- Each `<ul>` should have an accessible name that corresponds to the group label.

## Keyboard interactions

<KeyboardControls>
<KeyboardControl keyOrCombos="Enter / Spacebar / Down arrow">

- When the trigger has focus, pressing Enter, Spacebar, or the Down arrow opens the mega menu.
- The Down arrow additionally moves focus to the first mega menu item.

</KeyboardControl>

<KeyboardControl keyOrCombos="Tab">

- Focus starts on the first mega menu item and moves sequentially through each item in a group, then continues to the first item of the next group once the last item is reached.
- Tab includes interactive elements within custom regions in the focus sequence, based on their position in the layout. The custom region itself is not focusable.
- Exits and collapses menu: After the last focusable element, Tab moves focus out of the mega menu and collapses it.

</KeyboardControl>

<KeyboardControl keyOrCombos="Shift + Tab">

- If focus is in the mega menu, this action sets focus to the previous focusable component in the tab order.
- If the first element in the mega menu has focus, Shift+Tab moves focus to the previous focusable element outside of the mega menu.

</KeyboardControl>

<KeyboardControl keyOrCombos="Escape">

- If focus is in the mega menu, Escape closes the mega menu and returns focus to the triggering element.

</KeyboardControl>

<KeyboardControl keyOrCombos="Up arrow / Down arrow / Left arrow / Right arrow">

- Up and Down arrows move focus between items within a column.
- Left and Right arrows move focus to the first item of the previous or next column.
- Up on the first item of a column moves focus to the trigger and keeps the menu open.
- Left on the first column moves focus to the trigger and keeps the menu open. Right on the last column does the same, except on its last item: when there is a next trigger, it instead closes the mega menu panel and moves focus to that trigger.
- Down on the last item of a column moves focus to the first item of the next column. On the last item of the last column, it moves into the supporting actions row if one is present; otherwise, when there is a next trigger, it closes the mega menu panel and moves focus to that trigger, and does nothing when there is none.
- Within the supporting actions row beneath the groups, Left and Right move between actions, and Up returns focus to the last column. On the last action, Right behaves like the last item of the last column, and Down closes the mega menu panel and moves focus to the next trigger, or does nothing when there is none.

</KeyboardControl>

<KeyboardControl keyOrCombos="Home / End">

- Home moves focus to the first item in the current group.
- End moves focus to the last item in the current group.

</KeyboardControl>

</KeyboardControls>
