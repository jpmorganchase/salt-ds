---
title:
  $ref: ./#/title
layout: DetailComponent
sidebar:
  exclude: true
data:
  $ref: ./#/data
---

## Using the component

### When to use

- To represent multiple users, entities or objects that belong to the same group in a compact space.
- To summarize group membership when there isn’t enough room to show every member individually.

### When not to use

- To represent a single user, entity or object. Use [Avatar](/salt/components/avatar) instead.
- When every member must remain individually visible and actionable at all times.

## Import

To import `AvatarGroup` and related components from the lab Salt package, use:

```js
import { AvatarGroup, AvatarGroupCount } from "@salt-ds/lab";
```

## Props

### `AvatarGroup`

<PropsTable packageName="lab" componentName="AvatarGroup" />

### `AvatarGroupCount`

<PropsTable packageName="lab" componentName="AvatarGroupCount" />
