---
title:
  $ref: ./#/title
layout: DetailComponent
sidebar:
  exclude: true
data:
  $ref: ./#/data
---

## Default

The `Overlay` component allows the user to expose and interact with supplemental information. The overlay remains persistent until the user dismisses it.

<LivePreview componentName="overlay" exampleName="Default" />

## Placement

Use the `placement` prop to position overlay content to the `top`, `bottom`, `left` or `right` of the trigger element.

<LivePreview componentName="overlay" exampleName="Placement" />

## Sections

Use `OverlayHeader`, `OverlayPanelContent` and `OverlayFooter` to create distinct padded sections within `OverlayPanel`. Sections need to be direct children of `OverlayPanel` so their spacing rules apply; adjacent sections share a single `--salt-spacing-100` gap.

`OverlayHeader` follows the [Header Block pattern](/salt/patterns/header-block). Pass the title to `header`, add `preheader` or `description` when needed and put controls such as the close button in `actions`.

<LivePreview componentName="overlay" exampleName="WithFooter" />

## Long content

When content in `OverlayPanelContent` extends beyond its set height, it scrolls while the header and its close button stay in place.

<LivePreview componentName="overlay" exampleName="LongContent" />

## With actions

With actions, users can decide if interacting with content in the overlay should close the overlay. This example shows how activating the **Export** button triggers the export function, closes the overlay, and returns focus to the trigger element.

<LivePreview componentName="overlay" exampleName="WithActions" />

## Deprecated Close button

`OverlayPanelCloseButton` can be added as a child of the `OverlayPanel` component to render a close button in the top right corner of the OverlayPanel. You can pass the `onClick` prop like with any `Button` component to handle the close button click.

**Note:** `OverlayPanelCloseButton` is deprecated. Use the prop `actions` within `OverlayHeader` instead.

<LivePreview componentName="overlay" exampleName="CloseButton" />

## Hide arrow

Use the `hideArrow` prop to display the overlay without the arrow.

<LivePreview componentName="overlay" exampleName="HideArrow" />
