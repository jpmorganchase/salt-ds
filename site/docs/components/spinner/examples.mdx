---
title: Spinner
layout: DetailComponent
sidebar:
  label: Examples
data:
  $ref: ./#/data
---

<LivePreviewControls>
  <LivePreview componentName="spinner" exampleName="Size" >

    ### Size

    Spinners have two sizes, “default” and “large”. Large Spinners can be beneficial for full screen loading experiences; while default Spinners can be reserved for widget loading experiences. Set the `size` prop to `"default"` or `"large"` to change the size of the Spinner

  </LivePreview>
</LivePreviewControls>
