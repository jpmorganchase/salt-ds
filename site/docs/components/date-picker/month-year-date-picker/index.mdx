---
title: Month and year date picker
data:
  description: "`MonthYearSinglePanel` is composed alongside `DatePicker` and `DateInput` controls to let users select a single month/year value rather than a specific day."
  sourceCodeUrl: "https://github.com/jpmorganchase/salt-ds/blob/main/packages/date-components/src/month-year-panel"
  package:
    name: "@salt-ds/date-components"
  alsoKnownAs: []
  relatedComponents:
    [
      { name: "Date picker", relationship: "similarTo" },
      { name: "Date input", relationship: "contains" },
    ]
keywords:
  $ref: "#/data/alsoKnownAs"
layout: DetailComponent
sidebar:
  label: Month and year date picker
  groupLabel: Month and year date picker
  groupPriority: 3
---
