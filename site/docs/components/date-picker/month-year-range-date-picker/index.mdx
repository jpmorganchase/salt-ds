---
title: Month and year range date picker
data:
  description: "`MonthYearRangePanel` is composed alongside `DatePicker` and `DateInput` controls to let users select a month/year range rather than a specific start and end day."
  sourceCodeUrl: "https://github.com/jpmorganchase/salt-ds/blob/main/packages/date-components/src/month-year-panel"
  package:
    name: "@salt-ds/date-components"
  alsoKnownAs: []
  relatedComponents:
    [
      { name: "Range date picker", relationship: "similarTo" },
      { name: "Date input", relationship: "contains" },
    ]
keywords:
  $ref: "#/data/alsoKnownAs"
layout: DetailComponent
sidebar:
  label: Month and year range date picker
  groupLabel: Month and year range date picker
  groupPriority: 1
---
