---
title:
  $ref: ./#/title
layout: DetailComponent
sidebar:
  exclude: true
data:
  $ref: ./#/data
---

<Callout title="Note">

All date components require a [LocalizationProvider](/salt/components/localization-provider/examples) to be set up in your application.
For the purpose of the examples, we use type `DateFrameworkType` so the examples can be used across all date adapters. For your own code you can replace this type with the type of your date object (e.g luxon would use `DateTime` type instead).

</Callout>

## Default

`MonthYearSinglePanel` drops into the existing `DatePicker` composition and lets users pick a month and year rather than a specific day. The panel renders the twelve months of the visible year in a grid with previous/next year navigation. Provide a `parse` function on the input (see the example below) so users can also type values such as `January 2026` or `01/2026`.

<LivePreview
  componentName="date-picker"
  exampleName="SingleWithMonthYearPanel"
/>

## Min and max dates

Provide `minDate` and `maxDate` on the `DatePicker` to constrain the selectable months. Months whose full span falls outside the allowed window are rendered as disabled and skipped by keyboard navigation. Use `defaultVisibleYear` on `MonthYearSinglePanel` to open the panel on a year that contains a selectable month.

<LivePreview
  componentName="date-picker"
  exampleName="SingleWithMonthYearPanelMinMaxDate"
/>

## Disabled months

Use the `isMonthUnselectable` prop on `MonthYearSinglePanel` to disable individual months. The callback receives a date positioned at the first of the month and should return a rejection message when the month is unavailable, or `false` to allow it. The message is surfaced to assistive technology and shown as a tooltip on the month button. In the example below the third-quarter months (July, August and September) are unavailable.

<LivePreview
  componentName="date-picker"
  exampleName="SingleWithMonthYearPanelUnselectableMonths"
/>
