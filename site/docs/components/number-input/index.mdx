---
title: Number input
data:
  description: "`NumberInput` provides an editable field where users can enter numeric values. Users can manually enter their required number or increase/decrease the value using the controls or keyboard actions. This is particularly useful in scenarios where users need to make small adjustments to a value within a defined range."
  sourceCodeUrl: "https://github.com/jpmorganchase/salt-ds/blob/main/packages/lab/src/number-input/NumberInput.tsx"
  package:
    name: "@salt-ds/lab"
  alsoKnownAs: ["Spin Button", "Stepper Input"]
  relatedComponents: [{ name: "Input", relationship: "similarTo" }]

layout: DetailComponent
---
