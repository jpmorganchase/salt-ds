---
title:
  $ref: ./#/title
layout: DetailComponent
sidebar:
  exclude: true
data:
  $ref: ./#/data
---

## Default

The default kbd component displays a keyboard key in a visually distinct style.

<LivePreview componentName="kbd" exampleName="Default" />

## Variants

Kbd supports multiple background color variants: primary, secondary, and tertiary. These variants allow you to match different UI themes or highlight importance.

<LivePreview componentName="kbd" exampleName="Variants" />

## Inline with text

Kbd can be used inline within text to indicate keyboard shortcuts as part of a sentence.

<LivePreview componentName="kbd" exampleName="InlineWithText" />

## Nested in the button

Kbd can be nested inside a button to show the associated keyboard shortcut for an action.

<LivePreview componentName="kbd" exampleName="NestedInButton" />

## Nested in the input

Kbd can be placed inside an input field as an adornment to indicate a shortcut or expected key input.

<LivePreview componentName="kbd" exampleName="NestedInInput" />
