---
title:
  $ref: ./#/title
layout: DetailComponent
sidebar:
  exclude: true
data:
  $ref: ./#/data
---

## Default

Use the default link for navigation within the current tab.

<LivePreview componentName="link" exampleName="Default" />

## Open in a new tab

Use `target="_blank"` with `rel="noopener"` or `rel="noreferrer"` to open a link in a new tab. See [usage](./usage) for when to choose this behavior and [accessibility](./accessibility) for how the new-tab cue is announced.

<LivePreview componentName="link" exampleName="OpenInANewTab" />

## Color

To help create a visual hierarchy using a link, use the `color` prop. The default color is `primary`.

Set `color="inherit"` to inherit hover, active, and focus colors from the parent.

<LivePreview componentName="link" exampleName="Color" />

## Visited

Links change color after a user clicks on them. This tells the user which links they have already visited.

<LivePreview componentName="link" exampleName="Visited" />

## Underline

Use the `underline` prop to compare the default underline with `underline="never"`.

<LivePreview componentName="link" exampleName="Underline" />

## Routing libraries

Use the `render` prop to adapt `Link` to a routing library. This example renders a React Router link and maps `Link`’s `href` to React Router’s `to` prop.

<LivePreview componentName="link" exampleName="RoutingLibraries" />

## Variant

**Note:** This prop is deprecated. Use the `color` prop instead.

<LivePreview componentName="link" exampleName="Variant" />
