---
title: Form field
data:
  description: "`FormField` is a wrapper for user interface controls that are typically found in a form, such as `Input`, `ComboBox` or `RadioButton`. To make these controls accessible, you must provide them with a visible label, validation control, states and descriptive text elements."
  sourceCodeUrl: "https://github.com/jpmorganchase/salt-ds/blob/main/packages/core/src/form-field"
  package:
    name: "@salt-ds/core"
    initialVersion: "1.8.0"
  alsoKnownAs: ["Form input", "Input control", "Form element"]
  relatedPatterns: ["Forms"]
keywords:
  $ref: "#/data/alsoKnownAs"
layout: DetailComponent
---
