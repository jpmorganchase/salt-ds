---
title: List
data:
  description: The List component allows the user to select one or more items from an array of options—selected items are visually distinct. Long Lists are shown in a scrolling pane to provide access to all options in a space-efficient way.

  # Fill in the info from the content template's "Metadata" table below.
  # To omit optional items, comment them out with #
  sourceCodeUrl: "https://github.com/jpmorganchase/salt-ds/tree/add-component-template/packages/lab/src/list"
  package:
    name: "@salt-ds/core"
  alsoKnownAs: ["Listbox", "Menu", "Option List", "Selection menu"]
  # Add a { name: "...", relationship: "..." } block for each
  # related component here (separated by commas).
  # Permitted values for relationship are: "similarTo" or
  # "contains".
  relatedComponents:
    [
      { name: "Tree", relationship: "similarTo" },
      { name: "Vertical Navigation", relationship: "simlarTo" },
    ]
  # stickerSheet: "https://figma.com/..."

# Leave this as is
layout: DetailComponent
---

{/* This area stays blank */}
