---
title:
  $ref: ./#/title
layout: DetailComponent
sidebar:
  exclude: true
data:
  $ref: ./#/data
---

## Default

Use the `SemanticIconProvider` to replace any existing icon management setup with this provider and provide any custom icon mappings as required.

    <LivePreview componentName="semantic-icon-provider" exampleName="Default"  />

## Icon map

The `SemanticIconProvider` allows you to specify an `iconMap` to manage which icon components are used for various icon names. This provides flexibility to customize the icon set used in your application.

    <LivePreview componentName="semantic-icon-provider" exampleName="WithIconMap"  />
