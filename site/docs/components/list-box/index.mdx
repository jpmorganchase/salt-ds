---
title: List box
data:
  description: "`ListBox` allows the user to select an item from an array of options. Selected items are visually distinct from nonselected items. To ensure efficient space usage, long lists of items are in a scrolling pane that can provide access to options not immediately visible to the user."
  sourceCodeUrl: "https://github.com/jpmorganchase/salt-ds/blob/main/packages/lab/src/list-box"
  package:
    name: "@salt-ds/core"
  alsoKnownAs: ["Option List", "Selection List"]
  relatedComponents:
    [
      { name: "Combo box", relationship: "similarTo" },
      { name: "Dropdown", relationship: "similarTo" },
    ]
  relatedPatterns: ["List builder", "List filtering"]
keywords:
  $ref: "#/data/alsoKnownAs"
layout: DetailComponent
---

{/* This area stays blank */}
