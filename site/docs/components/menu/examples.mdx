---
title:
  $ref: ./#/title
layout: DetailComponent
sidebar:
  exclude: true
data:
  $ref: ./#/data
---

## Single level

Clicking on the menu trigger will display a list of actions that the user can take.

<LivePreview componentName="menu" exampleName="SingleLevel" />

## Multilevel

Menus can contain submenus which present the user with a set of items to interact with, creating a cascading effect.

### Best practices

- Creating submenus as separate components will allow you to reuse them in different parts of your application and make your code more maintainable.
- Limit the number of levels in your menu to avoid overwhelming users.

<LivePreview componentName="menu" exampleName="Multilevel" />

## Grouped items

Menu items can optionally be grouped together to organize related actions using a group label and separator.

<LivePreview componentName="menu" exampleName="GroupedItems" />

## Separator only

A `MenuGroup` can be used without a `label` prop to visually separate groups of menu items.

<LivePreview componentName="menu" exampleName="SeparatorOnly" />

## With icon

Icons can be used to help users quickly identify the type of action they can take.

### Best practices

- Icons should be displayed before a menu item label.
- Use icons consistently across all menu items within the same group.

<LivePreview componentName="menu" exampleName="WithIcon" />

## With badge

You can add a [badge](../badge) to a menu item to serve as a visual cue. A badge can inform users of additional context, notifications, signpost new content or indicate a status.

<LivePreview componentName="menu" exampleName="WithBadge" />

## Placement

Placement of the menu can be specified using `placement` prop. The menu will be positioned relative to child of the `MenuTrigger` component.
By default, the root menu is aligned to the "bottom-start" of the trigger element and submenus to the "right-start" of the parent menu item.

<LivePreview componentName="menu" exampleName="Placement" />

## Scrolling

When a menu extends beyond the application window, the menu defaults to span the maximum height of the viewport and scroll.

<LivePreview componentName="menu" exampleName="Scrolling" />

## Virtual element

A Menu can be shown relative to a virtual element using the `getVirtualElement` prop. This can be used to create a context menu.

<LivePreview componentName="menu" exampleName="VirtualElement" />

## Descriptions

You can add descriptions to provide extra context using the [`Text`](../text) and [`Stack Layout`](../stack-layout) components.

<LivePreview componentName="menu" exampleName="Descriptions" />
