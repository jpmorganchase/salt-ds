---
title:
  $ref: ./#/title
layout: DetailComponent
sidebar:
  exclude: true
data:
  $ref: ./#/data
---

## Brand theme overrides

Use `SaltProviderNext` instead of `SaltProvider` when you need to apply brand-aware overrides via the `accent`, `corner`, `actionFont`, or `headingFont` props.

<LivePreview componentName="salt-provider-next" exampleName="BrandTheme" />
