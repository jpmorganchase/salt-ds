---
title: Vertical navigation
description: "Vertical navigation provides users with a structured and intuitive way to navigate different pages, sections or features of your website or application. It's often on the left of the viewport and organizes links into a single or multilevel structure."
layout: DetailPattern
tags:
  - pattern
aliases:
  - /salt/patterns/vertical-navigation
data:
  resources:
    [
      { href: "https://storybook.saltdesignsystem.com/?path=/story/patterns-vertical-navigation", label: "Examples"},
    ]
  components: ["Vertical navigation"]
  relatedPatterns: ["Navigation"]
  ai:
    aliases:
      ["sidebar navigation", "navigation pane", "left-hand navigation", "nested navigation"]
    starterScaffold:
      sourceUrls: ["/salt/patterns/vertical-navigation"]
      template: "vertical-navigation"
      semantics:
        regions: ["navigation-pane", "nested-navigation", "content-context"]
        requiredRegions: ["navigation-pane"]
        optionalRegions: ["nested-navigation"]
        buildAround:
          [
            "VerticalNavigation",
            "VerticalNavigationItem",
            "Content context",
          ]
        preserveConstraints:
          [
            "Maintain a wide and flat hierarchy instead of one that is deep and overwhelming.",
            "Clicking a parent item won't navigate the user but expand the group.",
            "If an overview page is necessary it should be the first in the group.",
          ]
---

<Diagram
  src="/img/patterns/vertical-navigation/vertical-navigation-hero-shot.png"
  alt="A vertical navigation."
  border
/>

## When to use

Use vertical navigation when the page needs a persistent navigation pane that organizes multiple sections of an application into a vertical hierarchy.

Use it when:

- you need multiple top-level categories that represent the main sections of your website or application
- the information architecture is complex enough that a horizontal nav would be crowded
- the navigation should remain visible as the user moves through nested content
- clear labels or icons can help users scan the hierarchy quickly

## When not to use

- When the page only needs a small number of top-level destinations, use a simpler horizontal navigation or app header.
- When the navigation is secondary or page-local, use an anchor-style table of contents instead of a full navigation pane.
- When the content is primarily modal or task-oriented, use a dialog or drawer structure instead of a persistent sidebar.

## How to build

### Anatomy

A vertical navigation uses the `VerticalNavigation` component family in a persistent navigation pane:

1. [`VerticalNavigation`](/salt/components/vertical-navigation): Labels the navigation region and owns the item list.
2. [`VerticalNavigationItem`](/salt/components/vertical-navigation/examples): Represents a destination or expandable group. Only one destination should be active at a time.
3. [`VerticalNavigationSubMenu`](/salt/components/vertical-navigation/examples#collapsible-submenu): Contains nested items revealed by an expandable parent trigger. The parent expands the group rather than navigating.

You will typically use vertical navigation in a sidebar, on the left side of the screen, within a [drawer](/salt/components/drawer), or a [parent-child layout](/salt/components/layouts/parent-child-layout).

<Diagram
  src="/img/patterns/vertical-navigation/vertical-navigation-anatomy.png"
  alt="Annotated image of a vertical navigation."
  border
/>

<GuidanceCallout type="positive" customPillText="Best practices">
  You can use left-aligned icons to effectively signify the navigation item’s purpose, whether it’s related to a product’s identity, a specific tool, a function or a configuration setting.
</GuidanceCallout>

### Layout

`VerticalNavigation` owns the column layout and spacing between its items. Keep the hierarchy wide and flat.

<Diagram
  src="/img/patterns/vertical-navigation/vert-nav-layout.png"
  alt="Spacing for a vertical navigation"
  border
/>

## Group (single level)

A single-level vertical navigation group provides a simple list of links that users can select within an application or website. Use `VerticalNavigationItem` with a destination-backed trigger for each link.

<Diagram
  src="/img/patterns/vertical-navigation/vert-nav-default-group.png"
  alt="A single-level vertical navigation group"
  border
  caption="A single level vertical navigation group."
/>

## Nested group (multilevel)

Nest `VerticalNavigationItem` entries inside `VerticalNavigationSubMenu` to create a multilevel structure.

Top-level hierarchy items labels use a strong font weight, and nested items use a regular font weight.

<Diagram
  src="/img/patterns/vertical-navigation/vert-nav-nested.png"
  alt="A multi-level vertical navigation group"
  border
  caption="A multilevel vertical navigation group."
/>

<GuidanceCallout type="positive" customPillText="Best practices">
- Surface the hierarchy of your application’s information architecture to as few levels as possible. Maintain a wide and flat hierarchy instead of one that is deep and overwhelming.
- Clicking a parent item does not navigate the user but expands the group. If an overview page is necessary, make it the first item in the group. Overview pages do not include a chevron on the right-hand side.
</GuidanceCallout>

### Blur active state

When a parent item is collapsed but contains the currently active page, the parent is shown with the active indicator.

<ImageSwitcher
  images={[
    {
      src: "/img/patterns/vertical-navigation/blur-active-state-collapsed.png",
      alt: "Blur active state before it's opened",
    },
    {
      src: "/img/patterns/vertical-navigation/blur-active-state-expanded.png",
      alt: "Blur active state after it's opened",
    },
  ]}
  label="Expand to reveal active nested item"
  caption="This example demonstrates the blur-active state."
/>

## Secondary navigation

You can use `VerticalNavigationItem` triggers as anchor links within an application view, allowing users to navigate swiftly between various sections on a page. Typically, it functions as a top-level table of contents for the application.

We recommend using a density that is one size smaller than the main navigation. This is particularly applicable when:
 
- It isn't the primary means of navigation for the entire application and doesn't need the same level of prominence as, for instance, the left-hand navigation.
- A smaller density helps visually signify its hierarchy within the page.

<Diagram
  src="/img/patterns/vertical-navigation/secondary-navigation.png"
  alt="The secondary navigation demonstrated"
  border
  caption="Vertical navigation placed on the right of an application for secondary navigation."
/>

## Within modals

Modals such as preferences dialogs can contain a vertical navigation to allow users to navigate between distinct panels of content within the dialog.

Refer to the [preferences dialog](./preferences-dialog) pattern for further guidance on how to manage navigation within application preferences modals.

<Diagram
  src="/img/patterns/vertical-navigation/modal-vertical-navigation.png"
  alt="The vertical navigation within modals"
  border
  caption="Vertical navigation within a preferences dialog."
/>

## Responsive behavior

A vertical navigation remains fixed in width when the user resizes the browser. You can set it to the width of your choice, but you should consider the overall label length, theme, density, and screen size you are designing for when making your decision.

<ImageSwitcher
  images={[
    {
      src: "/img/patterns/vertical-navigation/vert-nav-responsive-behavior.png",
      alt: "A responsive layout when it's wide",
    },
    {
      src: "/img/patterns/vertical-navigation/vert-nav-viewport-change.png",
      alt: "A responsive layout when it's narrow",
    },
  ]}
  label="Change viewport size"
  caption="A vertical navigation responding to changes in viewport size."
/>

:fragment{src="./fragments/feedback.mdx"}
