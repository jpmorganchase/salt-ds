---
title: Themes
layout: DetailTechnical
sidebar:
  label: Overview
  groupLabel: Themes
  groupPriority: 4
---

A theme is a collection of [design tokens](/salt/themes/design-tokens) that define the overall look and feel of an interface. Themes allow you to quickly switch the visual style of components to match different brands or user preferences, ensuring consistency across your application.

## What is a Salt theme

In Salt, a theme is made up of tokens for colors, typography, shadows and corner radius. Themes help platforms and applications align with a specific visual identity and can be switched to suit different audiences.

<Image
  src="/img/theming/salt-themes-light.png"
  srcDark="/img/theming/salt-themes-dark.png"
  alt="A side-by-side comparison of the themes offered by Salt."
/>

## Which theme to pick

Salt offers four themes:

- **JPMorgan** (default)—_recommended for all new projects._
- **Legacy (UITK)**—designed to support the migration of applications from Salt’s predecessor.
- **Chase Consumer**—available exclusively to JPMorganChase employees for applications serving Chase customers.
- **JPMorganChase**—available exclusively to JPMorganChase employees for applications representing the parent brand. 

The long-term goal for Legacy users remains transitioning to the JPMorgan theme.

### JPMorgan theme

The JPMorgan theme is Salt’s long-term visual style, designed to match the JPMorgan brand across CIB digital applications. Developed with Corporate Brand and CIB Marketing, it uses approved styles for consistent visuals on desktop and web. Salt recommends using the JPMorgan theme for better accessibility and brand alignment.

### Legacy (UITK) theme

For a gradual move from the UITK system, Salt’s Legacy (UITK) theme can be used with UITK components to support different migration timelines and needs. J.P. Morgan employees can find more details on [migration options and system comparisons](https://go/saltds-migration).

### Chase theme

The Chase theme enables teams to build and deliver products within the Chase visual identity. Chase is available as a separate library in Figma and as a standalone package in code. JPMorganChase employees can find [installation instructions and setup guidance](https://go/saltds-chase-theme) internally.

### JPMC theme

The JPMC theme enables teams to build and deliver products aligned to the JPMorganChase parent brand. JPMC theme is available as a separate library in Figma and as a standalone package in code. 


## How to use themes in code

You can set themes in code using a provider and CSS imports. For a step-by-step walkthrough that covers installing the packages, adding web fonts and wrapping your application, see [Developing with Salt](../getting-started/developing).

### Setting up the JPMorgan theme

Import the base theme CSS plus the JPMorgan layer, then wrap your app in `<SaltProviderNext>`:

```tsx
import { SaltProviderNext } from "@salt-ds/core";
import "@salt-ds/theme/index.css";
import "@salt-ds/theme/css/theme-next.css";

<SaltProviderNext
  accent="teal"
  corner="rounded"
  headingFont="Amplitude"
  actionFont="Amplitude"
>
  {/* ... */}
</SaltProviderNext>;
```

The JPMorgan theme requires the **Amplitude** web font. JPMC employees can download the font files from [Salt internal resources](https://go/salt-ds-internal/resources/index), then add the following `@font-face` declarations to your application CSS:

<Disclosure
  id="amplitude-font-face"
  summary={<>View the Amplitude <code>@font-face</code> declarations</>}
>

```css
@font-face {
  font-family: Amplitude;
  font-style: normal;
  font-weight: 300;
  src:
    local("Amplitude Light"),
    url("../public/fonts/amplitude-light.woff2") format("woff2");
}

@font-face {
  font-family: Amplitude;
  font-style: normal;
  font-weight: 400;
  src:
    local("Amplitude"),
    url("../public/fonts/amplitude-regular.woff2") format("woff2");
}

@font-face {
  font-family: Amplitude;
  font-style: normal;
  font-weight: 500;
  src:
    local("Amplitude Medium"),
    url("../public/fonts/amplitude-medium.woff2") format("woff2");
}

@font-face {
  font-family: Amplitude;
  font-style: normal;
  font-weight: 700;
  src:
    local("Amplitude Bold"),
    url("../public/fonts/amplitude-bold.woff2") format("woff2");
}
```

</Disclosure>

### Setting up the Legacy (UITK) theme

The Legacy theme uses `<SaltProvider>` with the base theme CSS—no additional fonts or props are required. See [Using the Legacy theme](../getting-started/developing#using-the-legacy-uitk-theme) in Developing with Salt for the full setup.

## How to use themes in Figma

When using the Salt DS Figma libraries, you can use variable modes to change between 'JPMorgan' and ‘Legacy (UITK)'. All components, patterns and styles are compatible with each theme in Salt. The Chase and JPMC themes are provided as extended collections for internal use only.

<Image
  src="/img/theming/figma-themes-light.png"
  srcDark="/img/theming/figma-themes-dark.png"
  alt="A screenshot showing the theme menu in Figma."
/>

The Salt DS Themes library includes all the Figma variables needed to style with Salt. It includes the semantic characteristics, size and spacing values, shadows, corner radii and typographic styles.

The Figma variables are set up to match Salt tokens in code. When inspecting a design in Figma’s Dev Mode, you can see references that match the tokens, such as color, size and spacing values.

<Image
  src="/img/theming/figma-themes-devmode-light.png"
  srcDark="/img/theming/figma-themes-devmode-dark.png"
  alt="A screenshot showing Figma dev mode usage with Salt's button component."
/>

When referencing a design created using Salt with ‘View only’ Figma access, you’ll be able to see the Figma variables but not the token references. However, the variables match the same structure, so they should be relatively easy to translate.
