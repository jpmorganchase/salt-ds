import { RangeSlider } from "@salt-ds/core";
import type { ReactElement } from "react";

export const Default = (): ReactElement => (
  <RangeSlider aria-label="Default" style={{ width: "80%" }} />
);
