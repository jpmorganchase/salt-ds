import { FlowLayout, ToggleButton } from "@salt-ds/core";
import type { ReactElement } from "react";

export const AppearanceSolid = (): ReactElement => (
  <ToggleButton value="solid">Toggle</ToggleButton>
);
