import { Panel } from "@salt-ds/core";
import type { ReactElement } from "react";

export const ComponentExample = (): ReactElement => (
  <Panel variant="secondary">Live example of "Component Name"</Panel>
);
